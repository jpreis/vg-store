# adjust values and rename to .env

# env vars that should be available in the browser must be prefixed with NEXT_PUBLIC_ (see https://nextjs.org/docs/basic-features/environment-variables)
NEXT_PUBLIC_API_ENDPOINT="http://localhost:4444/api/graphql"